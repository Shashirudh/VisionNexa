# Model Weights Directory

Place your trained PyTorch Diabetic Retinopathy model weights (e.g. `dr_model.pth` or `best_model.pt`) in this directory.

> **Note**: Model weight binary files (`*.pt`, `*.pth`, `*.bin`, `*.onnx`) are ignored by Git via `.gitignore` to avoid bloating the repository.
